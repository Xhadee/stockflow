import 'package:flutter/material.dart';

class AddEditScreenProduct extends StatelessWidget {
  const AddEditScreenProduct({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Padding(
          padding: const EdgeInsets.all(24),
          child: const Text(
              "AddEditScreenProductPage"
          )
      ),
    );
  }
}